<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile</title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #fffaf5, #ffe8d6);
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
    }

    .card {
      max-width: 450px;
      background: linear-gradient(135deg, #fff5e6, #ffe6cc);
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(255, 94, 98, 0.2);
      animation: fadeIn 0.9s ease-in-out;
      border: none;
    }

    .profile-avatar {
      width: 110px;
      height: 110px;
      background: linear-gradient(135deg, #ff9966, #ff5e62);
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      box-shadow: 0 5px 15px rgba(255, 128, 8, 0.3);
      margin: 0 auto 15px;
    }

    .profile-avatar i {
      font-size: 4rem;
      color: white;
    }

    .info-box {
      background: white;
      border-radius: 12px;
      padding: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }

    .info-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
      padding: 0 10px;
    }

    .info-item:last-child {
      margin-bottom: 0;
    }

    .btn-primary {
      background: linear-gradient(90deg, #ff7b00, #ff9966);
      border: none;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #ff9966, #ff7b00);
    }

    .btn-outline-danger {
      border-radius: 50px;
      font-weight: 500;
    }

    .btn-outline-danger:hover {
      background-color: #ff5e62;
      color: white;
      border-color: #ff5e62;
    }

    .card:hover {
      transform: scale(1.02);
      transition: all 0.3s ease;
      box-shadow: 0 10px 25px rgba(255, 128, 8, 0.3);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="container d-flex justify-content-center align-items-center" style="min-height: 90vh;">
    <div class="card text-center p-4">
      <!-- Avatar -->
      <div class="profile-avatar">
        <i class="bi bi-person-circle"></i>
      </div>

      <!-- Name & Email -->
      <h3 class="fw-bold text-dark mb-1" style="font-family: 'Playfair Display', serif;">
        <%= user.name %>
      </h3>
      <p class="text-muted mb-3" style="font-size: 1rem;">
        <i class="bi bi-envelope-fill text-danger"></i> <%= user.email %>
      </p>

      <!-- Info Section -->
      <div class="info-box mb-3">
        <div class="info-item">
          <span class="fw-semibold text-secondary"><i class="bi bi-person-badge"></i> Role:</span>
          <span class="text-dark fw-bold"><%= user.role %></span>
        </div>
        <div class="info-item">
          <span class="fw-semibold text-secondary"><i class="bi bi-stars text-warning"></i> Rewards:</span>
          <span class="text-success fw-bold"><%= user.rewards %> pts</span>
        </div>
      </div>

      <!-- Buttons -->
      <div class="d-flex justify-content-center gap-3 mt-3">
        <a href="/restaurants" class="btn btn-primary px-4 py-2 rounded-pill shadow-sm">
          <i class="bi bi-arrow-left-circle me-1"></i> Back
        </a>
        <a href="/logout" class="btn btn-outline-danger px-4 py-2 rounded-pill shadow-sm">
          <i class="bi bi-box-arrow-right me-1"></i> Logout
        </a>
      </div>
    </div>
  </div>
</body>
</html>
