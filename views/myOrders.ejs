<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>📦 My Orders | Foodie</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #fef6e4, #fde7c2);
      font-family: 'Poppins', sans-serif;
      padding: 50px 15px;
      min-height: 100vh;
    }

    h2 {
      font-weight: 700;
      color: #ff7b00;
      text-shadow: 1px 1px 2px rgba(255, 123, 0, 0.2);
    }

    .order-card {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 18px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
      padding: 25px;
      margin-bottom: 25px;
      transition: transform 0.25s ease, box-shadow 0.25s ease;
      border-left: 6px solid #ff7b00;
    }

    .order-card:hover {
      transform: scale(1.02);
      box-shadow: 0 6px 25px rgba(0, 0, 0, 0.2);
    }

    .status {
      font-weight: 600;
      border-radius: 10px;
      padding: 5px 15px;
      display: inline-block;
    }

    .status.Placed {
      background-color: #cce5ff;
      color: #004085;
    }

    .status.Preparing {
      background-color: #fff3cd;
      color: #856404;
    }

    .status.ontheway {
      background-color: #d1ecf1;
      color: #0c5460;
    }

    .status.Delivered {
      background-color: #d4edda;
      color: #155724;
    }

    .status.Cancelled {
      background-color: #f8d7da;
      color: #721c24;
    }

    .btn-track {
      border-radius: 25px;
      background: linear-gradient(90deg, #009245, #00c9ff);
      color: white;
      padding: 8px 20px;
      text-decoration: none;
      font-weight: 600;
      border: none;
      transition: 0.3s ease;
    }

    .btn-track:hover {
      transform: scale(1.05);
      background: linear-gradient(90deg, #007e33, #00b4db);
    }

    .empty-state {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 18px;
      padding: 50px 20px;
      box-shadow: 0 6px 25px rgba(0, 0, 0, 0.15);
    }

    .btn-primary {
      background: linear-gradient(90deg, #ff7b00, #ff9966);
      border: none;
      font-weight: 600;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #ff6600, #ff8533);
    }
  </style>
</head>

<body>
  <div class="container">
    <h2 class="text-center mb-5">📦 My Orders</h2>

    <% if (!orders || orders.length === 0) { %>
      <div class="text-center empty-state">
        <h4 class="text-muted mb-3">No orders yet 😅</h4>
        <p class="text-secondary">Looks like you haven’t ordered anything yet!</p>
        <a href="/restaurants" class="btn btn-primary mt-3 px-4 py-2">Browse Restaurants 🍽️</a>
      </div>
    <% } else { %>
      <% orders.forEach(order => { 
          let statusClass = order.status.replace(/\s+/g, '').toLowerCase(); 
      %>
        <div class="order-card">
          <div class="d-flex justify-content-between align-items-start flex-wrap">
            <div>
              <h5 class="fw-bold text-dark mb-2"><%= order.restaurant_name %></h5>
              <p class="mb-1"><strong>Order ID:</strong> #<%= order.id %></p>
              <p class="mb-1"><strong>Total:</strong> ₹<%= order.total_amount- order.total_amount*0.05 %></p>
              <p class="mb-1"><strong>Payment:</strong> <%= order.payment_method %></p>
              <p class="mb-1"><strong>Date:</strong> <%= new Date(order.order_date).toLocaleString() %></p>
              <p class="mb-1"><strong>Status:</strong> 
                <span class="status <%= statusClass %>"><%= order.status %></span>
              </p>
            </div>

            <div class="text-end mt-3 mt-md-0">
              <a href="/track-order/<%= order.id %>" class="btn-track">🚚 Track Order</a>
            </div>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>

